import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  headeritem: {
    fontSize: "1.2rem",
    fontFamily: "serif",
    textDecoration: "none",
    cursor: "pointer",
    color: "#ffff",
  },
});
